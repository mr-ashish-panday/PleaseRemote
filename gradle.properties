# Project-wide Gradle settings

# Combined JVM args - memory settings + module access
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 --add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED --add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED --add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED

# Disable parallel execution temporarily
org.gradle.parallel=false

# Disable configuration cache temporarily
org.gradle.configuration-cache=false

# Keep build cache enabled
org.gradle.caching=true

# Android configurations
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin code style
kotlin.code.style=official

# JDK configuration (corrected path)
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.15.6-hotspot


